#include <iostream>
#include <cmath>
int main()
{

    double x = 3.99, y = 4, z;
    // z = std::max(x, y);
    // z=std::min(x,y);
    // z = pow(2, 30);
    //z = sqrt(9);
    //z=abs(-99);
    //z=round(x);
    //z=ceil(x);
    z=floor(x);
    std::cout << z;


    return 0;
}